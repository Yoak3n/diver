[package]
name = "diver-search"
version = "0.1.0"
edition = "2021"
description = "Diver grep 搜索后端（ripgrep 引擎库 crate）"

[dependencies]
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
grep-matcher = "0.1"
grep-regex = "0.1"
grep-searcher = "0.1"
ignore = "0.4"
