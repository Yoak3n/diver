/**
 * @cos/bundles — the named bundle aggregate + in-repo bundle packages for this
 * prototype harness. A bundle is a local directory that ships a declaration
 * and a patch list; the BootOptions.bundles aggregate composes them ordered.
 * @module @cos/bundles
 */

export {};
