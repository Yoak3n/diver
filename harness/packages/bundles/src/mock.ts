/**
 * @cos/bundles/mock — the "local mock provider" bundle, aggregate-resolved.
 * @module @cos/bundles/mock
 */

import type { Bundle } from '@cos/boot'

export const bundles: Array<string | Bundle> = ['@cos/bundle-mock']

export default bundles