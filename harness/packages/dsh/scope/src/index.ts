/**
 * `@deepseek-ai/dsh-scope` — DSH-compatible scope helpers on cos.
 * @module @deepseek-ai/dsh-scope
 */

export {
  bindScopeParent,
  carrierKeyOf,
  createScope,
  scopeChainOf,
  scopeOf,
  scopeTarget,
} from '@cos/scope'
export type { Scope, ScopeKey, Scoped } from '@cos/scope'
