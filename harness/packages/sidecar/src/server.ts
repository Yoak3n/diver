/**
 * @cos/sidecar/server — runnable dev/CLI entry for the JSON-RPC sidecar.
 * Boots with source watching (HMR) enabled; see sidecar.ts for the program and
 * sea.ts for the self-contained SEA entry.
 * @module @cos/sidecar/server
 */

import { main } from './sidecar'

void main()
