# Example secrets file for file-backed credentials (gitignored real one is
# secrets.yml, referenced by the credentials row's `config.file`).
# Copy this to secrets.yml and fill in real values; never commit the real file.
deepseek:
  apiKey: sk-REPLACE_ME
