// Live2D 桌宠渲染封装出口（行为与历史 `live2d.ts` 一致）。
// 拆分：moc / bounds / hitbox / layout / motion / expression / look / mouth / react / create。

export { createPetModel } from "./create";
export type { PetModelHandle, GroupAliases } from "./types";
